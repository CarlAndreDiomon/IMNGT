import SideBar from "../Components/SideBar"

export default function HomePage(){
    return(
        <main className="flex h-screen">
            <SideBar/>

            <h1>
                Home Page
            </h1>
        </main>
    )
}